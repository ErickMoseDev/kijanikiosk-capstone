apiVersion: v1
kind: ConfigMap
metadata:
    name: kk-payments-config
    namespace: default
    labels:
        app: kk-payments
        environment: production
data:
    NODE_ENV: 'production'
    DB_HOST: 'prod-db.internal'
    BUCKET_NAME: 'kk-payments-receipts-prod'
    PORT: '3000'
